# Sessione di sviluppo — 26 giugno 2026

## Cosa è stato implementato

### RouteDetailPage (`/routes/:routeId`) — riscrittura completa

Struttura:
- Breadcrumb: Falesie › Falesia › Settore › Via
- Header con nome, grado, link al settore/falesia, pulsante "Registra ascensione"
- Quick facts: grade, lunghezza, spit, tipo, tiri, inclinazione, roccia
- **Sicurezza** (sempre visibile): flags pericolo (primo spit alto, rischio terra/cengia, roccia instabile, catena), corda min., stato verifica
- **Attrezzatura** (sempre visibile): corda min/cons, rinvii min/cons, stick clip, casco
- **Grado community** (sempre visibile): grado ufficiale vs community, numero voti, form voto (grado percepito + stelle bellezza)
- **SpoilerGuard** — 3 sezioni protette:
  - Prese e movimenti (hold_profile, movement_profile, style_profile)
  - Crux e riposi (crux_data, rest_data)
  - Kneepad e kneebar (kneepad_data)
- Cronologia personale (ascensioni dell'utente su questa via)
- Form registra ascensione (toggle)

### SpoilerGuard (`src/features/routes/SpoilerGuard.tsx`)

- Contenuto spoiler **MAI nel DOM** prima dello sblocco (conditional render, non CSS)
- Dialog con avviso prima dello sblocco
- Checkbox "Ricorda per questa via" → salva in `route_spoiler_unlocks` (DB persistente)
- Utenti anonimi: sblocco solo per sessione, no "Ricorda"
- Accessibile da tastiera (aria-modal, autoFocus sul pulsante principale)

### Analytics (`src/analytics/`)

Moduli creati:
- `normalizers/grades.ts` — `numToGrade()`, `gradeToNum()`, `GRADE_SCALE`, `GRADE_TO_NUM`
- `types/index.ts` — `AnalyticsFilters`, `KpiData`, `GradeProgressionPoint`, `GradePyramidEntry`, `MonthlyActivity`, `CragStat`
- `filters/useAnalyticsFilters.ts` — hook con `setYear`, `setAttemptTypes`, `setGradeRange`, `reset`, `isDefault`
- `metrics/ascents.ts` — `filterAscents`, `computeKpis`, `computeGradeProgression`, `computeGradePyramid`, `computeMonthlyActivity`, `computeTopCrags`, `computeAvailableYears`

AnalyticsPage: 4 tab (Progressione, Volume, Profilo tecnico, Falesie), ScatterChart ascensioni nel tempo, piramide gradi orizzontale, top falesie, KPI.

### Hooks route (`src/features/routes/hooks.ts`)

- `useRouteFeatures(routeId)` — dati tecnici da `route_features`
- `useIsSpoilerUnlocked(routeId, userId)` — controlla `route_spoiler_unlocks`
- `useUnlockSpoiler()` — inserisce in `route_spoiler_unlocks` (ignora duplicati 23505)
- `useRoutePersonalHistory(routeId, userId)` — ascensioni utente su questa via
- `useCommunityRating(routeId)` — avg grado + avg bellezza + distribuzione
- `useMyRouteRating(routeId, userId)` — voto personale
- `useVoteRoute()` — upsert in `community_route_ratings`

### CSS

- `src/styles/route-detail.css` — nuovo file con tutti gli stili
- `src/styles/analytics.css` — aggiunto tabs, tooltip, filter bar, chart sections

### Migrazioni SQL (eseguite manualmente su Supabase)

| File | Contenuto |
|------|-----------|
| `010_route_features.sql` | Crea `route_features`, `route_spoiler_unlocks`, `community_route_ratings` con RLS |
| `011_ascent_style.sql` | Aggiunge `ascent_style`, `attempt_count`, `attempt_bucket`, `legacy_attempt_type`, `needs_review` ad `ascents` |

### Precedentemente (sessioni precedenti)

- **Sessioni page**: card espandibili con elenco vie
- **Le mie vie**: raggruppamento per grado, filtri, sort, pannello dettagli, stelle bellezza
- **Follow system**: `follows` table, `useIsFollowing`, `useFollowUser`, `useUnfollowUser`, `useFollowCounts`
- **Activity feed**: `useFeedAscents` in dashboard
- **Campo bellezza**: colonna `quality` su `ascents`, UI stelle in `AscentForm`
- **Redpoint**: aggiunto alle opzioni `ATTEMPT_OPTIONS` in `AscentForm`

---

## File modificati in questa sessione

```
src/analytics/normalizers/grades.ts        (nuovo)
src/analytics/types/index.ts               (nuovo)
src/analytics/filters/useAnalyticsFilters.ts (nuovo)
src/analytics/metrics/ascents.ts           (nuovo)
src/features/routes/SpoilerGuard.tsx       (nuovo)
src/features/routes/hooks.ts               (nuovo)
src/routes/RouteDetailPage.tsx             (riscritto)
src/routes/AnalyticsPage.tsx               (modificato — 4 tab + ScatterChart)
src/styles/route-detail.css                (nuovo)
src/styles/analytics.css                   (modificato — classi tab, tooltip)
src/types/database.ts                      (modificato — quality su Ascent)
src/features/logbook/hooks.ts              (modificato — quality in AscentFormValues)
src/features/logbook/AscentForm.tsx        (modificato — stelle bellezza, redpoint)
supabase/migrations/010_route_features.sql (nuovo)
supabase/migrations/011_ascent_style.sql   (nuovo)
```

---

## Problemi noti

### React error #62 (in corso di debug)
- Appare sulla build di produzione
- I POST a `fladifoai.github.io/[hash]` sono dall'estensione browser **SyncoRedux**, non dal codice
- Testare in finestra incognito per escludere interferenze estensioni
- Se persiste, specificare quale pagina mostra schermo bianco

### Supabase secrets (GitHub Actions)
- `VITE_SUPABASE_URL` e `VITE_SUPABASE_PUBLISHABLE_KEY` devono essere impostati in GitHub → Settings → Secrets
- In locale: creare `.env` con le stesse variabili (non committare mai)

---

## Stack tecnico

- React 19 + TypeScript strict + Vite 8
- TanStack Query v5
- Supabase Auth + PostgreSQL + RLS
- HashRouter (GitHub Pages)
- Recharts 3.x
- Deploy: GitHub Pages via GitHub Actions
- Branch: `master`
- URL prod: https://fladifoai.github.io/capital-climbing/
- Supabase project: `apfyktdacsklnptcgjko`
